# ===== Core =====
PAIR=SOL/USDT
EXCHANGE_ID=delta
DRY_RUN=true
MAX_LEVERAGE=10

# Sizing
SIZING_MODE=capital_frac         # capital_frac | risk_r | both
CAPITAL_FRACTION=0.5
RISK_PCT=0.75

TP_R_MULTIS=1.0,1.8,2.6
RSI_OB=80
RSI_OS=34
TASER_DB=taser.db
OPENAI_API_KEY=sk-xxxx

# Dynamic avoid (tuning, not zones)
DYN_AVOID_ENABLED=true
AVOID_LOOKBACK_BARS=120
CHOP_MIN_FLIPS=12
CHOP_MAX_WIDTH_PCT=0.006
CONF_MAX_SPREAD_PCT=0.004

# Scalper (AI runtime exits)
SCALP_ENABLED=true
SCALP_MIN_TICK_PROFIT=0.50
SCALP_MAX_TICK_PROFIT=0.75
SCALP_TIMEFRAME=1m
SCALP_MAX_HOLD_SECONDS=420

# Scheduler
SCAN_INTERVAL_SECONDS=300

# Telegram (optional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
